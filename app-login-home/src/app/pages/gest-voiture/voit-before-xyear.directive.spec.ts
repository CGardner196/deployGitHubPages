import { VoitBeforeXYearDirective } from './voit-before-xyear.directive';

describe('VoitBeforeXYearDirective', () => {
  it('should create an instance', () => {
    const directive = new VoitBeforeXYearDirective();
    expect(directive).toBeTruthy();
  });
});
