import { SoldVoiturePipe } from './sold-voiture.pipe';

describe('SoldVoiturePipe', () => {
  it('create an instance', () => {
    const pipe = new SoldVoiturePipe();
    expect(pipe).toBeTruthy();
  });
});
