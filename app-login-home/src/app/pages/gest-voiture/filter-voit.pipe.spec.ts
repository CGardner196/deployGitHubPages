import { FilterVoitPipe } from './filter-voit.pipe';

describe('FilterVoitPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterVoitPipe();
    expect(pipe).toBeTruthy();
  });
});
