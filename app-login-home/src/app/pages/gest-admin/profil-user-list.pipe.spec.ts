import { ProfilUserListPipe } from './profil-user-list.pipe';

describe('ProfilUserListPipe', () => {
  it('create an instance', () => {
    const pipe = new ProfilUserListPipe();
    expect(pipe).toBeTruthy();
  });
});
